#include "main.h"
/**
 * is_prime_number - prime number check area
 *@n: number to be checked if prime
 *Return: number checked
 */
int is_prime_number(int n)
{
	if (n <= 1)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
